# Frozen Role B classifier

Official primary-rack model: HistGradientBoosting, threshold **0.9**.

- Bundle: `models/primary_hgb_frozen.joblib` (created by `fpce-role-b-freeze`; **not tracked in Git** — share separately)
- Do not treat XGBoost artifacts under `reports/primary_xgb_*` as production
- Summary: `docs/role_b.md`
